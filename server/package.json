{
  "name": "peer-tree-server",
  "version": "1.1.0",
  "description": "A k-tree one-to-many network for WebRTC.",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "standard --fix src/** && tape test/*.js",
    "build": "browserify src/index.js -s NodeBoilerplate -o dist/bundle.js"
  },
  "author": "Thomas Mullen",
  "license": "MIT",
  "devDependencies": {
    "socket.io": "^2.0.3",
    "standard": "^8.6.0",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "simple-signal-server": "^1.5.0"
  }
}
